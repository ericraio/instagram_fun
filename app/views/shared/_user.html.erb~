<div id="<%= user.id %>" class="user">
	<p class="name"><%= link_to user.username, :controller => 'user', :action => 'index', :id => user.id %></p>
	<%= link_to( image_tag( user.profile_picture, :border => 0 ), :controller => 'user', :action => 'index', :id => user.id ) %>
</div>
